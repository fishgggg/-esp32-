# 02 · 遗留问题（截至当前）

| 问题 | 状态 | 说明 / 处理 |
|---|---|---|
| ESP32-CAM OV2640 数据总线故障 | 🔴 硬件 | 持续 `NO-SOI`（JPEG 起始标记缺失）。已排除：XCLK 频率 / 分辨率 / 缓冲模式 / PSRAM / SCCB（I²C 读写完美、PID=0x26 正确）。**彩条测试模式仍失败** → 并行数据总线(D0-D7/PCLK)物理不通。处理：更换 OV2640 模块或整板 |
| Web Speech API 国内不可用 | 🔴 | Google 云端被墙，Chrome / 手机浏览器均失败。已用**文字输入框兜底** |
| LED 硬件待焊接 | 🟡 | GPIO32/33/25 → 100Ω~220Ω 限流电阻 → LED → GND |
| App 分区仅剩 ~2% | 🟡 | 下次加功能需调整分区表 |
| PC 端 WiFi 重连不稳 | 🟡 | ESP32 复位后 Windows 经常不自动重连 SoftAP，不影响手机 |

## 项目状态一句话

**S1-S7 完成并验证（手机浏览器 Web 仪表盘通过），S8 文字控制已烧录验证、LED 待焊硬件，摄像头因硬件故障暂缺。**
