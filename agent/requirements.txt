# ============ 测试版本依赖 ============
PyYAML>=6.0          # 配置解析（demo 必需）

# ============ 真实模式才需要（可选安装） ============
# paho-mqtt>=2.0     # 真实 MQTT 订阅（python -m agent.main）
# requests>=2.31     # 真实 LLM 调用 / Server酱 推送
